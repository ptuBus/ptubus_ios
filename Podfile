# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ptuBus' do
  # Comment the next line if you don't want to use dynamic frameworks
  pod 'Alamofire', '~> 5.2'
  pod 'SwiftyJSON', '~> 4.0'
  use_frameworks!

  # Pods for ptuBus

  target 'ptuBusTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ptuBusUITests' do
    # Pods for testing
  end

end
